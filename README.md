# database-monitioring-docker
# SETUP FOR Grafana, Prometheus and node-exproter images in docker

# SETUp
## Navigate to the database-monitor folder in your CLI
## THEN RUN THE FOLLOWING COMMAND TO ADD TO DOCKER:
### docker-compose up -d
